# ok i want here to developp a package which rgroup a lot of funconnalities 
I use accross multiple project

Most of them implies ot download something from a s3 system 
and some basic operation like e rollup categoriesec.. 
thus i want to creatte function whic can be install like apackage in github via a pyproject.toml ok. 
I will guide you and give you some snippets but in a nutshelle Iwant t :


1) be able to downlad al the iles of a web map based  on a tile size ad a awebmap link 
2) download tiles in a given manifest  ( image and mask npxz) 
3) download a model based on its config  file 
4) be able to rollup anntation mask based on a remappng ?

# we will start by the web map import now : 
it relies on a function that i use which is call bbox to gri dansd some other parts 

# in the env fie i have everyuything we need so the use of thi function reauire a load dot env 



# after that annotation 
# model 